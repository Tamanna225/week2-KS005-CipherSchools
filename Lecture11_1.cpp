#include <iostream>

using namespace std;

// Memory elements in cpp

int main(){
    long long int a;    //it have bigger size than int
    int b;
    cout<<"The size of int is : "<<sizeof(int);     // WE get the size of int in bytes
    cout<<"The size of int is : "<<sizeof(b)<<" The size of long long int is: "<<sizeof(a);
    

    return 0;
}